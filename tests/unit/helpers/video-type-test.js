import { videoType } from 'gif-you-dare/helpers/video-type';
import { module, test } from 'qunit';

module('Unit | Helper | video type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = videoType([42]);
  assert.ok(result);
});
