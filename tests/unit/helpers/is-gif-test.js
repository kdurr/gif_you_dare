import { isGif } from 'gif-you-dare/helpers/is-gif';
import { module, test } from 'qunit';

module('Unit | Helper | is gif');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isGif([42]);
  assert.ok(result);
});
